export const latestProjectsConfig = {
  selectionDelayMs: 500,
  railCopyCount: 5,
  scrollSettleDelayMs: 180,
  mediaTransitionMs: 500,
} as const;

export const designProcessConfig = {
  initialStageIndex: 0,
  mediaTransitionMs: 500,
  accordionTransitionMs: 300,
} as const;

export const styleCompassConfig = {
  explorerTransitionMs: 560,
  selectionConfirmationDelayMs: 300,
  contentEnterDelayMs: 180,
  contentEnterDurationMs: 300,
  contentExitDurationMs: 150,
  mediaCloseDelayMs: 360,
  mediaCloseDurationMs: 200,
} as const;
